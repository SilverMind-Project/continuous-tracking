[pytest]
asyncio_mode = auto
pythonpath = tracking-orchestrator
empty_parameter_set_mark = fail_at_collect
testpaths =
    tracking-orchestrator/tests
filterwarnings =
    error::DeprecationWarning
markers =
    integration: integration tests requiring testcontainers
