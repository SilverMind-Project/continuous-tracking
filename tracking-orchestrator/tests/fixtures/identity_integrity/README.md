# Identity Integrity Characterization Fixtures

These fixtures are fully synthetic. Identity IDs, PH IDs, camera IDs, times, coordinates, and
vectors were created for tests. The directory must never contain household media, captured
embeddings, populated private manifests, credentials, or presigned URLs.

Strict xfails state the target invariant and identify the milestone that removes the xfail.

