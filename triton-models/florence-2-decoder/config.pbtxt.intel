# Florence-2 autoregressive decoder — ONNX Runtime + OpenVINO EP (Intel Arc GPU).
# Activated by: python triton-models/scripts/configure_gpu.py --vendor intel

name: "florence-2-decoder"
platform: "onnxruntime_onnx"
max_batch_size: 0

optimization {
  execution_accelerators {
    gpu_execution_accelerator : [
      {
        name : "openvino"
        parameters { key: "device_type" value: "GPU" }
      }
    ]
  }
}

instance_group [
  {
    kind: KIND_GPU
    count: 1
  }
]
